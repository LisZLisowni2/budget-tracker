version: '3.8'

services:
  frontend:
    build: ./frontend


  backend:
    build: ./backend
    env_file:
      - ./backend/.env

  nginx:
    build: /nginx
    

networks:
  overlayNet:
    driver: overlay